# Pixel-grounded broad progress semantics v7

ls20 is consumed development data after the valid v2 cross-game failure.
This version adds two generic capabilities:

1. When component correspondence finds no unique mover, exact pixel differencing
   may construct a multi-color action-correlated process from a persistent
   translated color signature. Complementary substrate restoration is retained
   but not mistaken for a richer multi-color process.
2. Any uniquely controlled process is a measurable progress opportunity, even
   when no collection capacity exists. Qwen must propose one of the same six
   generic families rather than receiving a collection-only opportunity bit.

This call is diagnostic: the inherited controller only executes collection
goals, so a semantically valid transformation/matching/etc. proposal will be
recorded but cannot yet complete the level. No source-derived goal or action
sequence enters the prompt.
