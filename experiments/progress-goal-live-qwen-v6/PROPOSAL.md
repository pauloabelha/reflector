# Goal-agnostic calibration regression v6

This version changes only the calibration substrate beneath successful v5.
Instead of inferring collection roles before acting, it tracks every visible
region through the common opaque-intervention prefix. Exact correspondences are
preserved; remaining equal-area/equal-bounds entities are matched by minimum
displacement to survive pose-dependent appearance changes. Controlled
candidates arise solely from action-correlated motion.

Qwen prompt, workspace goal vocabulary, geometric capacity projection,
progress-seeking rule, support-zero compiler, R2 open-port grounding, transport
controller, and <=40 action gate remain unchanged.

This fresh wa30 development regression must reproduce level1 and exact replay
without invoking the collection role grounder during calibration. Only then may
the same stack be frozen on a new metadata-selected game.
