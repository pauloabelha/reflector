# R2.1 Kaggle Breadth — Living Insights

This ledger separates observations, inferences, and promoted interventions.
Public games are development evidence only. A local score increase is not a
hidden-game competence claim unless its causal mechanism is auditable and
survives mechanically different games.

## I1 — Execution coverage precedes reasoning diagnosis

Observed:

- The inherited controller removes every complex action.
- A coordinate action is available in 19 of 25 initial public states; six
  games expose no usable simple action and stop at action zero.
- Exact click payloads are not represented in the current prediction,
  no-change, intervention-identity, or execution paths.

Inference:

- Zero score on those games is currently an interface failure, not evidence
  about perception, telos, planning, or causal induction.

Promotion requirement:

- A region-grounded coordinate command must preserve the exact payload from
  proposal through pending ledger, environment step, replay, and settlement.
- A dead coordinate must exclude only that command, never every click.
- Keyboard-only behavior must remain unchanged.

Status: implementation and contracts in progress; not promoted.

## I2 — Native R2 evidence was real but invisible to the aggregate

Observed:

- Earlier traces contained selected R2 predictions and confirmed settlements,
  while the inherited aggregate reported zero prediction objects and zero
  supported predictions.
- The inherited counter only recognizes graph predictions and environment
  support/refute edges.

Intervention:

- Selected eligible R2 predictions now enter the durable prediction graph and
  their immediate confirmed/refuted settlements use the existing environment
  adjudication path.
- A first implementation crossed the semantic quarantine through a control
  explanation dependency and was rejected. The corrected prediction depends
  only on semantic-safe frame evidence.

Evidence:

- A fresh `ar25` smoke crossed two action boundaries, recorded two prediction
  objects, and produced no semantic projection rejection.
- The current frozen `g50t` run recorded one prediction object per action
  through action 17 with no decision/execution mismatch.
- A later live audit showed confirmed/refuted judgments in
  `TransitionCommitted` but no graph edges: the inherited v1.9 filter requires
  the durable action proposal itself to name the selected prediction, while
  the original proposal had been frozen before the native R2 prediction was
  created.
- A corrected native proposal now selects the native prediction explicitly.
  A fresh nine-action `ar25` smoke produced five `supports` edges and one
  `refutes` edge, with environment as edge creator.

Status: prediction materialization promoted in `db1b3f7`; selected-proposal
ordering correction verified locally and awaiting frozen commit/breadth run.

## I2a — Parameterized mechanics must be executable before reasoning is judged

Observed:

- The inherited simple-action filter excluded every complex action. Six public
  games were guaranteed to stop immediately, and nineteen exposed a click
  action that R2 could not express.
- A fresh `ft09` smoke previously representative of that exclusion now
  executed `ACTION6` with grounded payload `{x: 6, y: 4}`. The exact payload is
  identical in selection, pending ledger, and committed successor, and the
  frame digest changed.

Inference:

- The zero-action failure was an interface/execution defect, not evidence that
  R2 could not infer the game's telos or mechanics.
- One successful transport does not show that the click was useful. Candidate
  generation and demand-driven role fitting remain potential efficiency and
  planning bottlenecks.

Status: execution transport candidate verified at a real boundary; awaiting
frozen breadth evidence for progress, latency, and cross-mechanic regressions.

Breadth regression:

- Both mixed-modality `lf52` and click-only `lp85` reached the new command path
  and then crashed on the same optional null `control_proposal` field.
- This is an execution/publication defect rather than game-specific evidence;
  the one-line normalization is protected by a direct controller contract.

## I3 — Exact no-change repetition was a control defect

Observed:

- A prior AR25 trace repeated the same action at the same predecessor five
  times with no visible change while the probe remained eligible.

Intervention:

- One no-visible-change closes that exact action/state probe until the state
  changes; advisory R2 rankings are separated from the executed selection.

Evidence:

- Contracts pass.
- The current frozen `g50t` trace has zero consecutive identical no-change
  probes through action 17 and zero selection/execution mismatch.

Status: promoted in `5a42f76`; broader action-efficiency evidence pending.

## I4 — Semantic repetition is not automatically semantic failure

Observed:

- The first valid `g50t` run repeatedly emitted the same `fit`/`align`
  proposal family while new transition evidence accumulated and no level was
  cleared.

Competing explanations:

1. Qwen is failing to revise an evidence-stale telos.
2. The telos is stable and useful while only the mechanism remains open; an
   identical semantic proposal is then correct and should not be churned.
3. Qwen is being scheduled when no semantic revision is demanded.

Discipline:

- Do not force cosmetic schema changes merely because another action occurred.
- A stagnation guard must rely on explicit semantic failure evidence or reduce
  redundant calls without retiring a still-grounded useful verb.

Intervention:

- Exact proposal-set repetition is rejected only after explicit unsupported R2
  grounding rejection or prediction refutation, and only when no confirmed or
  progress-eligible explanation suppresses that signal.
- Scheduler-only calls, open mechanisms, and mere new observations do not
  force semantic churn.

Status: contract-verified candidate; awaiting frozen breadth evidence that it
reduces stale fitting without discarding stable useful goals.

## I5 — Current breadth trace separates several failure layers

Observed in frozen `g50t` build `db1b3f7` through action 17:

- 17 executed actions and 17 durable predictions;
- 7 progress-eligible and 11 probe-eligible explanation records;
- 12 changed and 5 unchanged successor observations;
- zero identical no-change repetition and zero decision/execution mismatch;
- identity includes unique, uninitialized, and broken settlements;
- mechanism models include supported, unknown, and one contested state;
- no level clear yet.

Inference:

- Execution and decision publication are functioning on this keyboard game.
- The absent clear cannot yet be assigned uniquely to identity, mechanism,
  telos, or planning; all remain observable competing bottlenecks.
- Ordered animation exposure is still absent, so perception is only partially
  audited.

Status: live evidence; revisit at terminal outcome and across other mechanics.

## I5a — Deadline failures still require exact action accounting

Observed:

- The `g50t` worker timed out after 41 committed successors with one later
  `ActionPending`, but the parent summary recorded null actions.

Intervention:

- Interrupted outcomes now derive action and level counts exclusively from
  durable `TransitionCommitted` events and expose uncommitted pending actions
  separately.

Status: contract-verified telemetry correction; affects aggregate honesty, not
controller behavior or score.

## I6 — Structural transfer must remain probe-only

Observed:

- Exact effect keys retain palette, area, and raw shape, limiting reuse after
  palette/scale changes.
- Removing those fields naively would alias congruent actor and target roles
  and could manufacture false mechanics.

Safe hypothesis:

- A separate joint, role-ordered, palette/scale-normalized ledger may offer one
  probe only after unanimous agreement across at least two prior level
  contexts. Any conflict vetoes transfer. It cannot create progress authority
  or a fast path until locally confirmed.

Status: designed, not implemented; lower priority than execution coverage.

## I7 — Settled-frame control and ordered sensory evidence are distinct

Observed:

- The inherited observation and live-runtime normalizers repeatedly selected
  the last toolkit support and discarded every earlier support.
- The campaign classifier already distinguished ordered animation evidence,
  but replay could never expose it because observation blobs contained only
  the settled grid.

Intervention:

- Preserve the toolkit packet as an ordered, digested observation envelope in
  both live state and durable observation blobs.
- Continue to feed only its explicitly marked final support to existing R2.1
  control. Earlier supports are evidence, not automatically states, objects,
  motions, causes, or progress signals.

Inference:

- This repairs an observability boundary without adding game rules or granting
  control authority to an untested animation interpretation.
- Whether transient supports improve identity, mechanics, or planning remains
  an empirical question requiring real multi-support traces and a separate,
  evidence-bounded interpreter.

Status: contract-verified observability candidate; real packet prevalence and
decision impact remain unmeasured.
