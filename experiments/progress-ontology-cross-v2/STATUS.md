# Status

Frozen before constructing `ka59`; run finished.

The exact frozen treatment failed to produce a plan within depth 28 because its
parameterized-action addresses were restricted to a static side-pane cut.
Baseline also failed to complete level 1. The runner persisted the typed
no-plan outcome as `INVALID` rather than a valid abstention, repeating the v1
reporting limitation.

After preserving that result, ka59 became a consumed development target. The
generic address layer was broadened from side-pane components to all stable
motion-correlated objects and, critically, addresses were re-grounded after
every successor observation. With no source state or fixed coordinates in the
planner, exact observed-state search then completed level 1 in 11 factual
actions. A fresh replay reproduced the same terminal digest exactly. The search
used 123,132 planning interactions, so this is a mechanism/action-efficiency
breakthrough, not yet an interaction-efficient hidden-game result.
