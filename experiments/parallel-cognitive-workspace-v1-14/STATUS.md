# Parallel Cognitive Workspace v1.14 status

## 2026-08-09 — freeze ready

- v1.13 is preserved as a valid control-chain failure: both arms exact-replayed
  64 actions at level 0; the shared arm completed live proposal, grounding,
  probing, and evidence return but no unique revision/control.
- v1.14 contains no frozen or externally supplied task schema. Its only planned
  changes are complete grounding diagnostics, generic control-leverage guidance,
  and durable support-free feedback for rejected revisions.
- No v1.14 ARC environment has been opened and no fresh-workspace Qwen request
  made.
- Nineteen focused v1.14 tests pass, together with the inherited evidence and
  strict-response compatibility suites. Dry-run manifest construction and
  `git diff --check` pass.
- On the preserved v1.13 action-16 state used only as a codec/interface fixture,
  the complete diagnostic derived four unique predicates. Three selected an
  invariant co-moving pair; the sole leveraged row was `SameInteriorLayout` on
  the relative-moving pair. The resident Qwen copied that rank-1 predicate into
  a one-atom revision, cited both relation and prospective evidence, and the
  unchanged compiler accepted it with support zero.
- The preflight request used 12,097 prompt tokens and 1,142 completion tokens in
  the frozen 24,576-token context. It did not mutate an ARC environment or seed
  any v1.14 workspace. A fresh paired run remains the scientific test.
