# Status

INVALID infrastructure run. The process terminated during `lf52` before a
result or per-target checkpoint existed, after targets through `g50t` had been
opened. No scientific verdict is reconstructed and v2 will not be rerun.

The failure motivated shared-perception caching, bounded composition before
candidate allocation, and atomic per-target breadth checkpoints in v3.
