# Frozen cross-game progress-goal test v1 status

- Metadata-only selection frozen before environment open: `g50t-5849a774`.
- Runtime mechanism is the unchanged live-Qwen v5 stack.
- No g50t frame/source/outcome was consulted for this selection or adaptation.

## Frozen run — INVALID harness portability

- The durable metadata receipt was written before environment creation and
  resolved the frozen `g50t-5849a774` target.
- The fresh environment opened, but the inherited calibration harness called
  the wa30-era collection-role grounder before taking any action or invoking
  Qwen. g50t had no complete collection-role grounding, so the run terminated.
- This is not a model or game FAIL. It proves calibration was incorrectly
  downstream of a particular goal ontology. No cross-game result is claimed.
