# Conditional visual-route development gate v3

Fresh versioned correction after v2 executed 20 actions and stopped beside the
terminal.  v2 incorrectly exempted terminal-bound edges from the observed
route-color requirement.  v3 removes that exception: desire never overrides
topology evidence.  The evidence-supported route has 17 remaining macro moves;
with the existing four-action calibration and one expected blocked intervention
probe, the preregistered prediction is 22 actions.  All other code, Qwen
contract, budgets and gates remain unchanged.  No prior state is imported.
