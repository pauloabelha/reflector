# Conditional visual-route development gate v1

`tu93` is consumed development data after the frozen two-effect dispatcher
correctly rejected it.  The new generic class treats blocked actions as local
topology evidence and accepts control from any sparse action-correlated motion.
One successful displacement grounds actor size, macro node spacing, route
substrate and the distinct terminal marker.  R2 plans over the visible route,
probes unknown opaque interventions only when their direction is needed, and
replans after every observed movement.

Qwen receives the compact grounded route workspace and must write a support-zero
`ReachTerminal/RemainingRouteSteps` goal.  R2 owns topology and execution.
PASS requires level 1 within 24 actions and exact replay.  No game ID, palette,
coordinate, route, or action meaning appears in the cognitive contract.
