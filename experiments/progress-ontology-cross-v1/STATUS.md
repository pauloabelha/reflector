# Status

Frozen before constructing `dc22`; run finished.

The cycle arm exact-replayed 48 actions without completing level 1. The shared
arm correctly abstained after its four simple-action calibration transitions
because none of the three frozen effect families applied, but the first runner
reported that typed abstention as `INVALID` instead of persisting an episode
result. This is a harness reporting defect, not a model comparison.

After freezing and preserving that outcome, dc22 became a consumed development
target. Read-only source diagnosis showed that action 6 is parameterized and
edits the world's transition topology. A subsequent generic black-box visual
state search over simple actions plus connected-component click addresses
completed level 1 in 20 actions and exact-replayed to the same terminal digest.
That post-freeze development result belongs to
`progress-drive-editable-topology-v0`, not to this preregistered comparison.
