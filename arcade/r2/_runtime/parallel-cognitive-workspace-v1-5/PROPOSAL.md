# Parallel Cognitive Workspace v1.5

This is the separately versioned replacement for the v1.4 `INVALID` gate.
Every scientific and operational setting is unchanged: one fresh paired real
`ar25` level-1 experiment, 48 actions, live Qwen calls at logical sources
0/12/24/36 released eight actions later, prospective R2 probes, environment-only
support, evidence-citing non-alpha revision, confirmation-gated control, exact
replay, and post-episode same-state branches.

The only correction is transport-level. Prospective environment evidence no
longer duplicates the raw `action_id` inside Qwen's epistemic graph. The exact
action remains in the authoritative outer action ledger. The graph contains
the anonymous `intervention_ref` and dependencies to the exact prediction and
action-proposal objects, preserving causal addressability without revealing an
opaque action token to Qwen.

No v1.4 schema, response, graph object, trajectory, note, or outcome is an input.
The v1.5 workspaces and recordings must be fresh and empty.

