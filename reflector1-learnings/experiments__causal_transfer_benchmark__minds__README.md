# Mind artifacts

Frozen source Mind hashes and final transactional Mind hashes are recorded per target in `../independent_family/blind/blind_results.json`. Mind serialization is deterministic; the benchmark retains hashes rather than duplicating 96 reconstructions.
