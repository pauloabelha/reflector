# Parallel Cognitive Workspace v1.14 status

## 2026-08-09 — freeze ready

- v1.13 is preserved as a valid control-chain failure: both arms exact-replayed
  64 actions at level 0; the shared arm completed live proposal, grounding,
  probing, and evidence return but no unique revision/control.
- v1.14 contains no frozen or externally supplied task schema. Its only planned
  changes are complete grounding diagnostics, generic control-leverage guidance,
  and durable support-free feedback for rejected revisions.
- No v1.14 ARC environment has been opened and no fresh-workspace Qwen request
  made.
- Nineteen focused v1.14 tests pass, together with the inherited evidence and
  strict-response compatibility suites. Dry-run manifest construction and
  `git diff --check` pass.
- On the preserved v1.13 action-16 state used only as a codec/interface fixture,
  the complete diagnostic derived four unique predicates. Three selected an
  invariant co-moving pair; the sole leveraged row was `SameInteriorLayout` on
  the relative-moving pair. The resident Qwen copied that rank-1 predicate into
  a one-atom revision, cited both relation and prospective evidence, and the
  unchanged compiler accepted it with support zero.
- The preflight request used 12,097 prompt tokens and 1,142 completion tokens in
  the frozen 24,576-token context. It did not mutate an ARC environment or seed
  any v1.14 workspace. A fresh paired run remains the scientific test.

## 2026-08-09 12:59:11 — live census launched

- Jobs: 2; games: 1; profiles: 1; environment workers: 2.
- FAILED `generic_prospective/ar25/shared_live_qwen`: EpistemicGraphError: stable object identity was reused with different content.
- GLOBAL INVARIANT FAILURE: pending jobs cancelled; already-running workers may finish checkpoint boundaries.
- COMPLETE `generic_prospective/ar25/r2_only`: levels=0, actions=64, Q→R grounded=0, replay=True.

## 2026-08-09 — preserved mechanistic prefix and failure diagnosis

- v1.14 is **INVALID**, not a negative cognition result. The shared arm stopped
  after 25 committed actions on the correctly classified global invariant
  failure `stable object identity was reused with different content`.
- Before the stop, the full intended live chain occurred: Qwen's initial schema
  produced three bindings; R2 spent four ambiguity probes and returned eight
  supported predictions; Qwen revised to the one-atom `SameInteriorLayout ->
  Decrease TranslationAlignmentResidual`; R2 uniquely grounded `f01/f02`; the
  reserved confirmation probe was supported; and the next prospective-control
  decision selected opaque intervention 2 instead of fallback 5, predicting and
  observing residual 78 -> 72.
- The durable prefix contains two Qwen-to-R2 grounded pickups, nine environment
  support edges, the rejected repair attempt and compiler criticism, two Qwen
  derivations, four live bindings, and zero support-authority violations.
- Exact offline replay of the graph prefix identified object
  `eo:46badb0b...`, a `prospective-evidence-return` criticism. The confirmation
  probe packet had not changed on the following control transition (control
  evidence is independently durable but excluded from the probe packet), yet
  the bridge attempted the same criticism identity with a newer grounding-state
  payload/dependency. The graph correctly rejected the collision.
- Successor rule: if the cumulative probe packet is unchanged, reuse the prior
  criticism rather than rematerializing it from a later world state. A genuinely
  new packet still creates a new immutable criticism. No semantic/controller
  policy change is required.
